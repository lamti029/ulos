import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY COLORS
  static const Color primary = Color(0xFFFF9800); // Orange utama
  static const Color primaryLight = Color(0xFFFFB74D);
  static const Color primaryDark = Color(0xFFE65100);

  // SECONDARY (COKLAT)
  static const Color secondary = Color(0xFF795548); // Coklat utama
  static const Color secondaryLight = Color(0xFFA67C52);
  static const Color secondaryDark = Color(0xFF4E2A1B);

  // BACKGROUND
  static const Color background = Color(0xFFFFF6ED); // Soft putih cream
  static const Color surface = Colors.white;

  // TEXT
  static const Color textPrimary = Color(0xFF5D4037);
  static const Color textSecondary = Color(0xFF6D4C41);
  static const Color textLight = Colors.white;

  // STATUS
  static const Color success = Color(0xFF795548);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFC107);

  // LOCATION / TRACKING
  static const Color locationPin = Color(0xFFFF8A00);
  static const Color locationPulse = Color(0x33FF8A00); // transparan
  static const Color mapLine = Color(0xFFA67C52);

  // LOADING DOT
  static const Color dotActive = primary;
  static const Color dotInactive = Color(0xFFE0E0E0);

  // SHADOW
  // static const Color shadow = Color(0x29000000);
  static const Color cardShadow = Color(0x1A000000);
  static const Color accent = Color(0xFF212121);

  static const Color blue = Color(0xFF2196F3);
  static const Color green = Color(0xFF00FF00);
}
